---
title: "Computer-use RL environments"
summary: "Ten-plus production-grade environments that let an autonomous agent drive real enterprise web applications, built for training and evaluation."
context: "Scaler AI Labs"
period: "2026"
stack: ["Python", "FastAPI", "reinforcement learning"]
order: 4
nda: true
---

<NdaNote>

Employer-internal work built for external customers. The applications targeted are described by category rather than by name, and the recipients are not named. If any of this is cleared for public description, the specifics are worth restoring — they are what makes the scope legible.

</NdaNote>

## Problem

An agent that operates a computer the way a person does — reading the screen, clicking, typing —
cannot be trained or evaluated against a static dataset. It needs an environment: a real application
it can act on, with a defined starting state, a way to observe the result, and a way to judge whether
a task was completed.

I built more than ten of these against real enterprise web applications spanning observability, ERP,
accounting, and creative tooling. The engineering problem is less about any single application than
about making a heterogeneous set of them behave like a uniform interface an agent and a training loop
can both rely on.

## Constraints

- Deterministic starting state. A task that begins from a different state each run cannot produce a
  comparable score.
- Reproducibility across runs and machines, because the output was consumed by other teams as a
  benchmark.
- The applications were third-party and not built for this, so every environment had to work through
  the interface the application actually presents.

<Todo>

Add the real constraints: were environments required to run headless, inside containers, with a fixed wall-clock budget per episode? Was there a required schema each environment had to conform to?

</Todo>

## Approach

<Todo>

This is the section that carries the page, and it is the one I cannot write for you without inventing. Worth covering:

- What did the environment interface look like — reset, step, observation, termination?
- How was starting state established and restored between episodes: fixtures, snapshots, seeded accounts?
- How was task success determined — asserting on application state, or on the agent&rsquo;s trace?
- What did FastAPI serve here: the environment itself, an orchestration layer, or both?
- How much was shared scaffolding versus per-application work? A rough split makes the scale of &ldquo;ten-plus&rdquo; concrete.

</Todo>

<DiagramSlot label="Environment interface: agent → action → application under test → observation (screen + structured state) → reward/termination signal, with the reset path that restores a known starting state. Draw this once the interface is pinned down." />

## Tradeoffs

<Todo>

The interesting tension in this kind of work is fidelity versus stability: driving the real application is faithful but breaks when the vendor ships a UI change, while stubbing it is stable but teaches the agent something slightly false. Where did you land, and what broke?

</Todo>

## What I would do differently

<Todo>

One concrete decision you would reverse. If the per-application work turned out to dominate the shared scaffolding, that is a strong and specific answer.

</Todo>
